apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "staging-locatorapp.fullname" . }}-test-connection"
  labels:
    {{- include "staging-locatorapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "staging-locatorapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
